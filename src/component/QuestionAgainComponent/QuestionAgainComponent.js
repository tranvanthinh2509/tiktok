import Button from '../Layout/component/Button/Button';

function QuestionAgainComponent({ children }) {
    return <div className="fixed z-30 top-0 left-0 w-full h-screen bg-black bg-opacity-10 ">{children}</div>;
}

export default QuestionAgainComponent;
